#include "main.h"
#include <stdio.h>
/**
 * print_diagsums - entry
 * @a: int to be checked
 * @size: to checked
 * Return: Always 0
 */
void print_diagsums(int *a, int size)
{
	int sum1, sum2, y;

	sum1 = 0;
	sum2 = 0;

	for (y = 0; y < size; y++)
	{
		sum1 = sum1 
	}

}
