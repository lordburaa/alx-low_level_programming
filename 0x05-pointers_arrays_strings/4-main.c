#include "main.h"
/**
 * main - check the code
 * Return: Always 0
 */
int main()
{
	char *str;
	str = "I do not fear computers. Ifear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
