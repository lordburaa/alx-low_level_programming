#include "main.h"
/**
 *
 *
 */
char *cap_string(char *str)
{
	int index = 0;

	while (str[undex])
	{
		while (!(str[index] >= 'a' && str[index] <= 'z'))
			index++;
		if (str[index - 1] == ' ' || str[index -
	}
}

