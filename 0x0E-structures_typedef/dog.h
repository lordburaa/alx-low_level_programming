#include "dog.h"
#include <stdlib.h>
/**
 * struct dog - owner
 * @name: name
 * @age: age
 * @owner: owner
 * Return : 0
 */
void init_dog(struct dog *P, char 8name, float, char *owner)
{
	if (s == NULL)
		d =malloc(sizeof(struct dog));
	d->name = name;
	d->age = age;
	d->owwner = owner;
}

