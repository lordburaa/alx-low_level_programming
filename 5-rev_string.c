#include "main.h"
/**
 * rev_string - reverse string
 * @s: string to be modified
 * Return: void
 */
void rev_string(char *s)
{
	int l, idx;
	l = 0;
	idx = 0;
	while (s[idx++])
		l++;
	for (idx = l - 1; idx >= len / 2; idx
}
